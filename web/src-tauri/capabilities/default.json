{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the zmkay main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
